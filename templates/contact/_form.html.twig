{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{{ form_start(form) }}
{{ form_widget(form) }}

<button class="btn btn-success" style="float: left; margin-right: 5px">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}